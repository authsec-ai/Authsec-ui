/**
 * @deprecated `PhaseStub` was used during the Phase 1–3 rollout to
 * placehold routes that hadn't shipped yet. All Launch Control tabs now
 * have real pages; nothing imports this component. The file remains only
 * because the workspace's file-deletion approval service blocked the rm.
 * It can safely be deleted in the next housekeeping pass.
 */

export {};
