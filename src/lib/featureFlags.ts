/**
 * @deprecated Launch Control is now the default UI. This module previously
 * gated the new IA behind `VITE_FEATURE_LAUNCH_CONTROL`; the flag has been
 * promoted to permanent behaviour and the import sites removed. The file
 * remains only because the workspace's file-deletion approval service
 * blocked the rm. It can safely be deleted in the next housekeeping pass.
 */

export {};
